/**
 * Robot Menus
 * For defining menu items for MainMenu, DemosMenu, TestsMenu, CalibrationMenu
 * in their own classes.
 * 
 * <p>
 * Each of the existing menus have their own class, extending the SubMenu, and
 * having a method that adds all the other menu items to their internal lists.
 * Get the main menu object by the method {@code MainMenu.getInstance();}, which
 * display on the ev3, the other three menus as options to open.
 */
package ca.mcgill.ecse211.project.menu;